[package]
name = "engine"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
wgpu = { version = "0.19", default-features = false, features = ["webgpu"], optional = true }
petgraph = "0.6"

[features]
default = []
webgpu = ["wgpu"]
